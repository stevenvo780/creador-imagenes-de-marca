# Motor CSS/HTML → PNG (headless browser)
playwright>=1.40

# Utilidades base
Pillow>=10.0
numpy>=1.24

# Base de datos: SQLite (default) o Postgres (via DATABASE_URL)
psycopg[binary]>=3.1

# Almacenamiento GCS (webapp backend Cloud Run)
google-cloud-storage>=2.14

# ELIMINADO (difusión GPU — ya no se usa):
# torch
# diffusers
# transformers
# accelerate
# safetensors
