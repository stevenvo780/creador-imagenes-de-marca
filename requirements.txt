# Motor CSS/HTML → PNG (headless browser)
playwright>=1.40

# Utilidades base
Pillow>=10.0
numpy>=1.24

# ELIMINADO (difusión GPU — ya no se usa):
# torch
# diffusers
# transformers
# accelerate
# safetensors
