/**
 * Página del wizard para crear batch combinatorio.
 * Utiliza el nuevo WizardFlow multi-paso.
 */
import { WizardFlow } from "./Wizard";

export function BatchWizardPage() {
  return <WizardFlow />;
}
